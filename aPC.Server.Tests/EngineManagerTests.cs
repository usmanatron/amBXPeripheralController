namespace aPC.Server.Tests
{
  internal class EngineManagerTests
  {
    /* Difficult to test this as it messes with the amBX library. That said, would be good if I could?
     * At the least I should test the conversion stuff (or, even better, work out a better way of comparison)
     */
  }
}